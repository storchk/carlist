{
  "name": "carlist",
  "author": {
    "name": "Kevin Storch"
  },
  "version": "1.0.0",
  "scripts": {
    "start:backend": "pnpm run --prefix backend dev",
    "start:frontend": "pnpm run --prefix frontend dev",
    "install:frontend": "pnpm i --prefix frontend",
    "install:backend": "pnpm i --prefix backend"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.5",
    "@commitlint/config-conventional": "^17.6.5",
    "husky": "^8.0.3",
    "prettier": "^2.8.8"
  }
}
